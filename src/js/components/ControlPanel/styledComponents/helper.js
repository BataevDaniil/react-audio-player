export const sideMainTriangle = ({ heightTriangle }) =>
	heightTriangle * Math.sqrt(3) / 2.0;

export const sideTransparentTriangle = ({ heightTriangle }) =>
	heightTriangle / 2;

export const widthLineTriangle = ({ heightTriangle }) =>
	heightTriangle / 6.0;
